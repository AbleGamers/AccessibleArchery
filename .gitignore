# Godot 4+ specific ignores
.godot/

# Godot-specific ignores
.import/
export.cfg
# NOTE: export_presets.cfg is deliberately COMMITTED here (CI exports need it).
# That's safe because our presets hold no signing identities or secrets —
# re-ignore it if credentials are ever added to a preset.

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Builds / exports
/build/
/export/
/android/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# AI-assistant orientation notes — kept local to each machine, not published.
CLAUDE.md

# Voice bridge: local Python venv and downloaded speech models (large, per-machine)
.venv/
/models/
